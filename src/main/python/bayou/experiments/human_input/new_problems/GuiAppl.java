import java.io.*;
import java.util.*;
import javax.swing.*;


class MyGuiAppl{

	public JFrame createFrame(final String title){
	  JFrame frame=new JFrame(title);
	  return frame;
	}


	public JButton newButton(String t){
	  super(checkBox);
	  button=new JButton();
	  button.setOpaque(true);
	  bListener=l;
	  title=t;
	}

        /**
        add closing action to JButton and add to JFrame
        */
        public void actionClose(JButton a, JFrame f){
            __PDB_FILL__(); 
        }
}

